// /password/resetRequest - Page where users can request to reset their password, given an email

import PassResetRequestForm from "@/components/PassResetRequestForm/PassResetRequestForm";

const PassResetRequest: React.FC = () => {
  return <PassResetRequestForm />;
};

export default PassResetRequest;
