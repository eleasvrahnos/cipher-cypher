// /login - Page where users can log in to the site to access their data

import LoginForm from "@/components/LoginForm/LoginForm";

const LoginPage: React.FC = () => {
  return <LoginForm />;
};

export default LoginPage;
